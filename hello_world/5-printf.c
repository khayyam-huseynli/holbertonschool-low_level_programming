#include <stdio.h>
/**
 * main - This prints given string with printf function
 *
 * Return: returns (0) case of Success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
